AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Journey Continuity deterministic cloud-contact watchdog

Parameters:
  ScheduleState:
    Type: String
    Default: DISABLED
    AllowedValues: [ENABLED, DISABLED]
  SmsViewerUrl:
    Type: String
    Description: HTTPS trusted-viewer base URL included without tokens in factual SMS alerts
  SmsOriginationIdentity:
    Type: String
    Description: Existing End User Messaging SMS sender ID, phone identity, or pool identity
  SmsConfigurationSetName:
    Type: String
    Default: ''
    Description: Optional existing End User Messaging SMS configuration set
  SmsDispatchBatchSize:
    Type: Number
    Default: 10
    MinValue: 1
    MaxValue: 25

Resources:
  WatchdogSecret:
    Type: AWS::SecretsManager::Secret
    Properties:
      Description: Supabase URL and server-side sb_secret key for the Journey watchdog

  WatchdogLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub /aws/lambda/${AWS::StackName}-watchdog
      RetentionInDays: 14

  WatchdogLambdaRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: WatchdogRuntime
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource: !GetAtt WatchdogLogGroup.Arn
              - Effect: Allow
                Action: secretsmanager:GetSecretValue
                Resource: !Ref WatchdogSecret
              - Effect: Allow
                Action: sms-voice:SendTextMessage
                Resource: '*'

  WatchdogFunction:
    Type: AWS::Serverless::Function
    DependsOn: WatchdogLogGroup
    Properties:
      FunctionName: !Sub ${AWS::StackName}-watchdog
      CodeUri: function/
      Handler: index.handler
      Runtime: nodejs22.x
      Architectures: [arm64]
      MemorySize: 128
      Timeout: 30
      Role: !GetAtt WatchdogLambdaRole.Arn
      Environment:
        Variables:
          WATCHDOG_SECRET_ARN: !Ref WatchdogSecret
          SMS_VIEWER_URL: !Ref SmsViewerUrl
          SMS_ORIGINATION_IDENTITY: !Ref SmsOriginationIdentity
          SMS_CONFIGURATION_SET_NAME: !Ref SmsConfigurationSetName
          SMS_DISPATCH_BATCH_SIZE: !Ref SmsDispatchBatchSize

  SchedulerExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: scheduler.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: InvokeWatchdog
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action: lambda:InvokeFunction
                Resource: !GetAtt WatchdogFunction.Arn

  WatchdogSchedule:
    Type: AWS::Scheduler::Schedule
    Properties:
      Description: One shared minute-resolution Journey watchdog evaluation
      ScheduleExpression: rate(1 minute)
      FlexibleTimeWindow:
        Mode: 'OFF'
      State: !Ref ScheduleState
      Target:
        Arn: !GetAtt WatchdogFunction.Arn
        RoleArn: !GetAtt SchedulerExecutionRole.Arn
        Input: '{"source":"journey-continuity.watchdog"}'
        RetryPolicy:
          MaximumEventAgeInSeconds: 300
          MaximumRetryAttempts: 2

  SchedulerInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref WatchdogFunction
      Principal: scheduler.amazonaws.com
      SourceArn: !GetAtt WatchdogSchedule.Arn

Outputs:
  WatchdogSecretArn:
    Description: Populate this secret with SUPABASE_URL and SUPABASE_SECRET_KEY before enabling the schedule
    Value: !Ref WatchdogSecret
  WatchdogScheduleName:
    Value: !Ref WatchdogSchedule

