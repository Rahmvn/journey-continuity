# Generate each value from 32 cryptographically random bytes and Base64URL-encode it.
# Retain old versions in the key ring while any stored record references them.
FALLBACK_KEY_KEKS_JSON={"1":"REPLACE_WITH_32_BYTE_BASE64URL_KEK"}
FALLBACK_ACTIVE_KEK_VERSION=1
